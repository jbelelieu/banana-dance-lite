<?php
if (sizeof($var['innerHeadings']) > 0) {
    echo '<ul id="innerHeadings">';
    foreach ($var['innerHeadings'] as $aName => $data) {
        echo '<li class="' . $data['class'] . '"><a href="#' . $aName . '">' . $data['name'] . '</a></li>';
    }
    echo '</ul>';
}
?>

{{ content }}

<p id="timestamps"><!--Created on {{ created }} and-->Last modified {{ timeSinceModified }} ago.</p>